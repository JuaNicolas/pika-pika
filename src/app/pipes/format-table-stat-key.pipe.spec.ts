import { FormatTableStatKeyPipe } from './format-table-stat-key.pipe';

describe('FormatTableStatKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTableStatKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
