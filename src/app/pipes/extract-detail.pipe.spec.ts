import { ExtractDetailPipe } from './extract-detail.pipe';

describe('ExtractDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
